<%= render "city"  if !@city.nil? %>
<%= render "level" if !@level.nil? %>

<%= form_tag(search_url, :method => "post") do %>
  <input id="query" class="input-medium search-query" name="query" placeholder="Ciudad, Nivel o Centro" type="search" />
  <%= submit_tag "Buscar" %>
<% end %> 
