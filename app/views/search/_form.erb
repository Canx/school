<%= form_tag("/results", :method => "get") do %>
  <%= search_field_tag("query", params[:query]) %>
  <%= select_tag(:level, options_from_collection_for_select(Level.all, :id, :name)) %>
  <%= submit_tag "Buscar" %>
<% end %> 
